# 轻淘 — 管理员功能指南

> 管理员后台 `/admin`，仅 `role=admin` 用户可访问

---

## 目录
1. [核心变化：审核模型](#1-核心变化审核模型)
2. [数据概览](#2-数据概览)
3. [图片审核](#3-图片审核)
4. [用户管理](#4-用户管理)
5. [举报处理](#5-举报处理)
6. [分类管理](#6-分类管理)
7. [公告管理](#7-公告管理)
8. [轮播图管理](#8-轮播图管理)

---

## 1. 核心变化：审核模型

### 旧模型（v1.x）
```
用户发布 → 内容进入 pending 状态 → 管理员审核（通过/拒绝）→ 公开展示/退回
```
→ 用户体验差：发布后要等审核，不知道什么时候能通过

### 新模型（v2.0）
```
用户发布 → 内容立即上架，其他用户立即可见
         ↓
    有图片 → 图片自动模糊处理（高斯模糊 + 低分辨率）
         ↓
    管理员审核图片合规性
    ├─ 通过 → 图片变清晰
    └─ 拒绝 → 对应内容被下架，通知作者
```

### 审核范围对比

| 审核对象 | v1.x | v2.0 |
|----------|:---:|:---:|
| 商品内容 | 需审核 | ❌ 直接上架 |
| 商品图片 | — | ✅ 审核图片合规 |
| 帖子内容 | 需审核 | ❌ 直接上架 |
| 帖子图片 | — | ✅ 审核图片合规 |
| 失物招领内容 | 需审核 | ❌ 直接上架 |
| 失物图片 | — | ✅ 审核图片合规 |
| 商品评论 | 需审核 | ❌ 直接上架 |
| 帖子评论 | 需审核 | ❌ 直接上架 |
| 用户资料修改 | 需审核 | ❌ 直接生效 |
| 聊天图片 | — | ✅ 审核图片合规 |

### 图片模糊机制

- 用户上传图片 → 后端 Sharp 生成两个版本：
  - `url` — 原始清晰图片（存储但暂不对外展示）
  - `blurredUrl` — 模糊版（高斯模糊 + 缩放到 50px 再放大，形成像素化效果）
- 前端默认加载 `blurredUrl`
- 审核通过 → 前端切换加载 `url`
- 审核拒绝 → 内容下架，通知作者"图片不合规，商品已下架"

---

## 2. 数据概览

**路由**: `/admin`

### 统计卡片
| 卡片 | 说明 |
|------|------|
| 总用户数 | active 用户总数 |
| 总商品数 | 在售商品总数 |
| 总帖子数 | 未删除帖子总数 |
| 今日新增用户 | 今天注册的用户数 |

### 快捷入口
| 入口 | 跳转 |
|------|------|
| 图片审核 | `/admin/images` — 待审图片数 |
| 举报管理 | `/admin/reports` — 待处理举报数 |
| 用户管理 | `/admin/users` |
| 发布公告 | 弹窗/独立页 |

---

## 3. 图片审核

**路由**: `/admin/images`

> 管理员唯一的审核工作：审核用户上传的图片是否合规

### 审核队列
- 按时间倒序排列
- 每条审核项展示：

| 字段 | 说明 |
|------|------|
| 模糊预览图 | 展示当前模糊效果 |
| 清晰预览图 | 点击可放大查看原图 |
| 上传者 | 用户昵称 |
| 来源 | 商品 / 帖子 / 失物招领 / 聊天 / 头像 |
| 关联内容 | 点击跳转到对应商品/帖子详情 |
| 上传时间 | 相对时间 |
| 状态 | 待审核 / 已通过 / 已拒绝 |

### 操作

**通过**
- 点击"通过"→ 图片状态 → `approved`
- 前端自动切换为清晰图片
- 通知上传者"图片审核通过"

**拒绝**
- 点击"拒绝"→ 弹出原因输入框 → 提交
- 图片状态 → `rejected`
- **关联内容自动下架**（status → offline）
- 通知上传者"图片审核未通过：{原因}，商品已下架，修改图片后可重新上架"
- 用户可编辑商品更换图片后重新上架

### 审核标准（建议）
- ✅ 正常的商品实拍图、帖子配图
- ❌ 色情低俗图片
- ❌ 暴力血腥图片
- ❌ 违法违规内容（毒品、枪支等）
- ❌ 二维码/广告引流图
- ❌ 无关网图/表情包（作为商品主图时）

---

## 4. 用户管理

**路由**: `/admin/users`

### 用户列表
- 搜索（按用户名/昵称）
- 分页列表（每页 20 人）

| 列 | 说明 |
|------|------|
| 头像 | 首字母头像 |
| 用户名 | 唯一标识 |
| 昵称 | 显示名称 |
| 角色 | 管理员标签 |
| 状态 | 活跃 / 已冻结 |
| 注册时间 | 相对时间 |

### 操作

**冻结用户**
- 用户状态 → `disabled`
- 冻结效果：所有内容自动下架 + 购物车清空 + 无法登录
- 不可冻结管理员

**解冻用户**
- 恢复为 `active`，用户可正常登录

**删除用户**
- 确认弹窗 → 禁用账号 + 软删除所有内容 + 清空购物车
- 不可删除管理员

---

## 5. 举报处理

**路由**: `/admin/reports`

### 举报列表
- 分页（每页 20 条）

| 列 | 说明 |
|------|------|
| 举报人 | 昵称 |
| 举报类型 | goods / post / lostfound / user |
| 目标 ID | 被举报内容链接 |
| 原因 | 用户填写 |
| 状态 | 待处理 / 已处理 / 已驳回 |
| 时间 | 举报时间 |

### 处理操作
- **已处理** → 标记 handled，记录处理人
- **驳回** → 标记 dismissed（举报不成立）
- 处理后可附带备注（可选）

---

## 6. 分类管理

**路由**: 独立页面或在 Dashboard 中操作

- 分类列表（名称 + 图标 + 排序 + 商品数）
- 新增 / 编辑 / 删除
- 删除分类前检查是否有商品使用
- 分类影响：首页 8 个快捷分类 + 商品列表 20 个分类

---

## 7. 公告管理

### 发布公告
- 标题（必填）+ 内容（选填）
- 发布后 → 给所有 active 用户发送通知
- 游标分批（每批 500 人），避免内存溢出
- 通知类型：`announcement`

### 公告效果
- 所有用户收到通知"管理员公告：{标题}"
- 在通知中心可查看
- 公告本身有 isActive 字段，可停用

---

## 8. 轮播图管理

### 首页轮播图
- 图片列表：缩略图 + URL + 排序 + 启用状态
- 添加：URL + 排序值
- 删除：直接移除
- 最多 5 张（当前设计）
- 图片格式建议：WebP，16:8 桌面 / 16:9 移动

---

## 附录：管理员 API

```
GET    /api/admin/stats                     → 数据概览
GET    /api/admin/images?status=&page=      → 图片审核队列（新增）
POST   /api/admin/images/:id/approve        → 图片审核通过
POST   /api/admin/images/:id/reject         → 图片审核拒绝 { reason }
GET    /api/admin/users?keyword=&page=      → 用户列表
PATCH  /api/admin/users/:id/status          → 冻结/解冻
DELETE /api/admin/users/:id                 → 删除用户
POST   /api/admin/categories                → 新增分类
PUT    /api/admin/categories/:id            → 编辑分类
DELETE /api/admin/categories/:id            → 删除分类
POST   /api/admin/announcements             → 发布公告
GET    /api/admin/reports?page=             → 举报列表
POST   /api/admin/reports/:id/handle        → 处理举报
GET    /api/admin/banners                   → 轮播图列表
POST   /api/admin/banners                   → 添加轮播图
PUT    /api/admin/banners/:id               → 编辑轮播图
DELETE /api/admin/banners/:id               → 删除轮播图
```

> **废弃的 API**（v1.x 审核系统，将被移除）：
> `GET /api/admin/reviews`、`POST /api/admin/reviews/:type/:id/approve`、`POST /api/admin/reviews/:type/:id/reject`
