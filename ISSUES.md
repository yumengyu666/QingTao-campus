# 轻淘 Campus — 五轮用户测试问题汇总

> 测试角色：郑州轻工业大学在校生
> 测试日期：2026-06-01
> 测试范围：注册/登录、首页、二手市场、广场、失物招领、树洞、恋爱专区、私聊、答疑、考试资料、购物车、收藏、拉黑、举报、管理员后台、个人中心、移动端、后端安全、数据库、Token、文件上传、并发、性能

---

## P0 — 安全/功能缺口（18 项）

### #1 恋爱请求全程无通知
- **文件**: `qingtao-server/src/controllers/dating.controller.ts`
- **现象**: 用户发起恋爱请求 → 对方不知道；对方接受/拒绝 → 发起方不知道。只能手动点进弹窗刷新看状态。
- **根因**: `sendRequest` 和 `handleRequest` 均未调用 `createNotification`。

### #2 私信无通知 + 导航栏无消息红点
- **文件**: `qingtao-server/src/controllers/messages.controller.ts`、`qing-tao-campus/src/stores/unreadStore.ts`、`qing-tao-campus/src/components/layout/SideNav.tsx`
- **现象**: 别人发私信 → 收件人完全不知道。导航栏"消息"图标无红点。必须主动点进消息页才能发现新对话。
- **根因**: 后端无 `getUnreadCount` 函数、`sendMessage` 不产生通知；前端 `unreadStore` 只有通知计数、SideNav 无消息轮询。

### #3 恋爱关注无法取消 + 无关注列表接口
- **文件**: `qingtao-server/src/routes/dating.routes.ts`、`qingtao-server/src/controllers/dating.controller.ts`
- **现象**: 点了"关注"后按钮变"已关注"，无取消关注功能。手滑关注错人只能永远关注着。
- **根因**: dating routes 无 `DELETE /:userId/follow`、无 `GET /following`。

### #4 Q&A（答疑专区）零 AI 审核
- **文件**: `qingtao-server/src/controllers/qa.controller.ts`、`qingtao-server/src/routes/qa.routes.ts`
- **现象**: 有人在回答里贴"代考四六级"信息，明晃晃挂着没人管。
- **根因**: 路由层未挂 `moderateBody`，controller 零 `aiModerate`、零 `afterCreate`、零 `createNotification`。

### #5 通知中心类型缺失
- **文件**: 多个 controller
- **现象**: 通知中心只有评论通知。恋爱请求、私信消息、AI 审核结果——均无通知。
- **根因**: 仅 `goods/post/lostfound/images` 调用了 `createNotification`。

### #6 登录无暴力破解防护
- **文件**: `qingtao-server/src/routes/auth.routes.ts`
- **现象**: 连续输错密码 10 次，每次都返回 401，无次数限制。
- **根因**: 无针对性 `loginLimiter`。

### #7 前端路由 `/admin` 不拦截非管理员
- **文件**: 前端路由配置
- **现象**: 普通用户在浏览器地址栏敲 `/admin`，页面能进去（空壳）。
- **根因**: 前端路由层未做角色校验/重定向。

### #8 删除/编辑接口作者校验需逐接口确认
- **现象**: 编辑商品被 403 拦截，但删除商品、下架/上架商品是否校验作者未确认。

### #9 AI 审核静默失效
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: `MODERATION_API_KEY` 为假值或 `MODERATION_API_URL` 指向无效地址时，AI 审核静默失败。内容正常发出，用户和管理员均无感知。首次失效无日志标明进入熔断。
- **根因**: `aiModerate` 失败时只 `return 'unknown'`，无告警、无管理员通知。

### #10 文件上传不验证真实文件类型
- **文件**: `qingtao-server/src/middleware/upload.ts`
- **现象**: `.exe` 改名为 `.pdf` 上传，后端仅检查扩展名，接受假文件。存在恶意文件上传风险。

### #11 URL 可遍历（ID 递增）导致越权访问
- **文件**: 多个 controller（商品、帖子、私信、用户）
- **现象**: `/goods/122` 可看到他人待审核商品。私信 URL `/messages/15` 暴露 userId，改数字可向任意用户发私信。帖子 ID 递增可暴力遍历。

### #12 分类/用户被删除后引用悬空 → 前端白屏
- **文件**: 各列表页组件
- **现象**: 管理员删除分类 → 商品卡片分类显示为空。用户被硬删除 → 其帖子和商品的 `userId` 悬空 → 前端渲染抛错 → 整个列表页白屏。

### #13 Token 无设备绑定、可被复制
- **文件**: auth 中间件、前端 authStore
- **现象**: 浏览器控制台获取完整 JWT，复制到另一浏览器直接登录。公共电脑登录后未退出，下一个使用者可拿到 token 冒充。无 IP/User-Agent 绑定。

### #14 跨设备同时登录无感知
- **文件**: 后端 auth 机制
- **现象**: 同一账号在手机和电脑同时登录互不冲突。若他人获取 token，受害人不知道自己账号被同时使用。

### #15 树洞无发帖频率限制
- **文件**: `qingtao-server/src/routes/treehole.routes.ts`
- **现象**: 一分钟可发一百条垃圾帖子灌满树洞。

### #16 恋爱关系无法断开
- **文件**: `qingtao-server/src/controllers/dating.controller.ts`
- **现象**: 接受恋爱请求后联系方式永久暴露，无法"断开关系"或撤回已暴露的联系方式。

### #17 无忘记密码功能
- **文件**: 前端登录页、后端 auth controller
- **现象**: 登录页无"忘记密码"入口。密码丢失 → 账号永久作废。

### #18 无注销账号功能
- **文件**: 前端个人中心、后端 user controller
- **现象**: 用户无法自行删除账号，微信/QQ 联系方式永久挂在平台上。

### #19 无交易评价体系
- **文件**: 后端无 Review/Rating 模型
- **现象**: 交易完成后无法互评。买家爽快无法给好评、卖家放鸽子无人知晓。整个交易无信用体系。

### #20 无手机号/邮箱绑定 → 账号无法找回
- **文件**: User 模型、注册页、个人中心
- **现象**: 注册仅需学号+密码，无手机号/邮箱绑定。密码丢失账号永久作废、被盗号后无法验证身份找回。

---

## P1 — 体验硬伤（33 项）

### #19 版本号过旧
- **文件**: `qing-tao-campus/src/pages/profile/MyProfilePage.tsx:259`
- **现象**: 底部显示 `轻淘 v1.2.0`，与实际功能严重不匹配。

### #20 购物车无去重
- **现象**: 同一个商品反复点"加入购物车"，购物车出现两条记录，无"已在购物车中"提示。

### #21 无黑名单管理页面
- **现象**: 拉黑了人之后，找不到黑名单列表，无法取消拉黑。

### #22 树洞无举报入口
- **文件**: `qing-tao-campus/src/pages/treehole/TreeHolePage.tsx`
- **现象**: 匿名区帖子和评论没有任何举报按钮。

### #23 管理员后台无文字内容审核
- **文件**: `qing-tao-campus/src/pages/admin/AdminDashboard.tsx`
- **现象**: 管理员只能审核图片，不能审核文字内容。

### #24 全局无头像上传
- **现象**: 注册时无头像上传、设置里无换头像入口。全校用户头像全是一个默认占位图。

### #25 联系方式暴露无门槛
- **文件**: `qingtao-campus/src/pages/goods/GoodsDetailPage.tsx`、恋爱专区
- **现象**: 商品详情页直接显示卖家微信/QQ。恋爱请求接受后联系方式解锁——反悔了收不回来。

### #26 注册成功无提示 + 封禁无原因提示
- **现象**: 注册成功静默跳转登录页。被封禁账号登录失败无原因说明（仅有 URL 参数 `?banned=1`）。

### #27 收藏不随商品删除清理
- **现象**: 商品被卖家删除后，收藏列表里该条目还在——点进去显示"商品不存在"。

### #28 通知不随资源删除清理
- **现象**: 帖子被删除后评论通知还在——点进去 404。

### #29 编辑模式下图片不回显
- **文件**: `PublishGoodsPage.tsx`、`PublishPostPage.tsx`
- **现象**: 编辑已有商品/帖子时，之前上传的图片不在编辑框中显示。

### #30 连续点击无防抖
- **现象**: 快速点击"发布"两次 → 创建两个同样的商品。快速点赞两次 → 计数 +2。

### #31 退出登录后浏览器后退可见缓存数据
- **现象**: 退出后按浏览器后退键，仍能看到聊天记录和个人资料（DOM 缓存，API 已 401）。

### #32 离线/网络异常页面白屏无提示
- **现象**: 后端服务关闭 → 前端页面一片白。

### #33 通知不聚合、不显示具体对象
- **现象**: 通知列表只显示"有人评论了你的商品"，不告诉是哪一件。有 5 件商品上架收到 3 条评论，得逐个点开确认。

### #34 通知无一键全部已读
- **现象**: 30+ 条通知积压，必须逐条点击。点完还要手动刷新页面才能消红点。

### #35 标记售出后不通知买家/收藏者
- **现象**: 商品标记"已售出"，之前问过价的买家、收藏过的人不知道，几天后还来问"东西还在吗"。

### #36 聊天记录排序不友好
- **现象**: 聊天记录从最早一条开始显示，每次打开要往下翻很久才能看到最新消息。

### #37 消息发送失败无重试提示
- **现象**: 网络卡顿 → 前端显示发送成功但消息实际未送达 → 无"发送失败，点击重发"提示。

### #38 树洞点赞可刷（localStorage 可清除）
- **现象**: 清除浏览器缓存/AI 私聊 localStorage 后又能继续点赞。点赞数虚高。

### #39 无"已有恋爱对象"约束
- **现象**: 已接受恋爱请求后，仍可向第三人发起请求，第三人也可向你发起请求。

### #40 恋爱帖子不可编辑删除
- **现象**: 恋爱区帖子发出去后不能编辑、不能删除，打错字永远挂着。

### #41 考试资料无评价举报
- **现象**: 有人上传虚假试卷（2019 年的标成 2024 年），无评价或举报机制来标记。

### #42 文件上传超大文件崩溃
- **现象**: 20MB PDF 上传 → 后端直接 500，无友好错误提示。

### #43 文件上传特殊文件名乱码
- **现象**: `期末试卷(2024)最终版!!.pdf` 上传成功，下载时文件名乱码，浏览器无法下载。

### #44 多标签页状态不同步
- **现象**: 标签页 A 填发布表单；标签页 B 退出登录 → 标签页 A 表单还在，提交后 toast 一闪即灭，数据丢失。购物车同理：B 清空后 A 显示已过时数据。

### #45 前端无限滚动不回收内存
- **现象**: 树洞翻几十页、聊天 500 条消息——全部留在内存不回收。越翻越卡，手机端明显掉帧。

### #46 首页轮播图静态无法更换
- **现象**: 首页轮播图一个月没变过。管理员后台无更新 banner 的入口。

### #47 首页"最新"和"热门"Tab 无区分
- **现象**: 两个 Tab 展示内容高度重复。热度计数规则不透明，新发布的都是 0 热度 → 最新 = 热门。

### #48 校区筛选不明显 → 跨校区面交成本不可控
- **现象**: 数据有 campus 字段但筛选 UI 不明显或缺失。科学校区和东风校区两校区物品混合显示，面交可能需坐 40 分钟校车。失物招领里更严重——白跑一趟。

### #49 无浏览器推送通知（Notification API）
- **现象**: Web App 无浏览器推送。用户不打开 App 就完全不知道有人联系自己。所有社交互动靠用户主动"查看"。

### #50 私聊不支持发图片
- **现象**: 聊天界面只能发文字。买卖双方想确认商品成色必须跳出 App 用微信/QQ 传图。

### #51 发帖状态不可见（发送中/发送失败）
- **现象**: 点击发布后网络请求发出但响应没收回来——用户不知道到底发没发成功。无"发送中…"状态、无失败重试提示。只能看帖子列表有没有出现新帖来判断。

---

## P2 — 优化（39 项）

### #46 验证码无重试次数限制
- 数学验证码答错可无限重试，存在脚本暴力猜解风险。

### #47 搜索无历史记录
- 首页搜索关键词不保存，之前搜过什么完全不记得。

### #48 搜索只能搜商品
- 搜索结果仅商品，无帖子、失物招领、答疑内容。

### #49 搜索大小写敏感
- 搜 "iphone" 搜不到 "iPhone 13"，搜索不够智能。

### #50 搜索不支持类型筛选
- 搜"高数"结果是商品和帖子混在一起，不能只看商品或只看帖子。

### #51 图片上传无进度条
- 大图上传时干等数秒，无任何进度反馈。

### #52 图片无拖拽排序
- 多图上传后无法调整图片顺序。

### #53 商品图片无 lightbox 全屏浏览
- 点击图片只是原位置放大，想仔细看细节看不清。

### #54 时间显示为 ISO 时间戳
- 评论、消息、通知全是 `2025-06-01T14:30:00.000Z`，无"3分钟前""昨天"等相对时间。

### #55 分页器简陋
- 只有"上一页/下一页"，无"跳转到第 N 页"输入框，无"共 X 页"提示。

### #56 移动端键盘遮挡输入框
- 底部导航栏被键盘顶上，发评论/私信时看不到输入内容。

### #57 移动端弹窗过大、关闭按钮难触达
- 恋爱请求弹窗在手机上特别大，关闭按钮在左上角，单手够不到。

### #58 移动端商品图片留白过多
- 商品详情页图片在手机上只有一半宽度。

### #59 图片加载失败无占位图
- 浏览器显示默认裂图图标。

### #60 购物车无快捷联系入口
- 已加购后想联系卖家，还得点回商品详情页。

### #61 收藏列表不能操作
- 收藏列表纯展示，不能直接联系卖家或加入购物车。

### #62 分类筛选交互不直观
- 选中分类后无法取消回到"全部"，必须找到"全部"标签点击。

### #63 价格筛选无格式校验
- 输入 "abc" 点筛选无校验提示，结果变空。

### #64 我的商品/帖子无搜索筛选
- 历史记录多时只能一页一页翻。

### #65 举报处理结果无反馈
- 提交后只有"举报成功"，不知道平台是否处理。

### #66 公告用户端不可见
- 管理员发了公告，普通用户在首页、通知中心都看不到。

### #67 恋爱私聊与普通私聊割裂
- 恋爱区私聊和"消息"页私聊是两个独立入口，同一个人两边聊天记录互不相通。

### #68 考试资料不可编辑
- 上传后发现文件名写错，不能改。上传前也无预览确认。

### #69 表单无草稿保存
- 手机切后台再回来页面刷新，填了一半的发帖内容丢失。

### #70 普通关注和恋爱关注完全割裂
- 在广场关注了一个人，去恋爱区还显示"未关注"。同一个人两套关注系统独立。

### #71 通知无偏好设置
- 无法关闭不想要的通知类型（如评论通知），全部通知一样对待。

### #72 生产代码残留 console.log
- 前端构建产物中有调试日志，F12 可见 API 请求参数和内部 ID。

### #73 校区字段显示不一致
- 个人资料选"科学校区""东风校区"，商品发布页面显示拼音 kexue / dongfeng。

### #74 API 地址硬编码
- 前端 API 地址写死在代码中，换域名部署需改代码重新打包。

### #75 昵称修改不同步历史内容
- 用户改昵称后，之前的聊天记录、评论里还是旧昵称。同一段对话里对同一人的称呼不一致。

### #76 聊天不支持消息搜索
- 想回看之前聊过的内容，只能凭记忆一页一页往回翻。

### #77 聊天翻页体验反人类
- 加载更多消息的按钮在消息列表最上面（不是最下面），不符合聊天习惯。

### #78 无"查看卖家其他商品"入口
- 买家问"你还有什么别的在卖"，只能让对方去主页翻。用户主页只是列表，不是展示橱窗。

### #79 首页无限滚动无底部提示
- 下拉到底无"我是有底线的"提示，用户以为没加载完一直拉。

### #80 通知标题内容截断
- 通知列表超过一定字数直接截断，必须点进详情页才能看全。每条通知都要跳转查看，效率极低。

### #81 无排行榜/达人标识
- 想知道全校最活跃用户、交易量最多的人——没有任何排行或达人机制。恋爱区也没有"最受欢迎"的内容发现。

### #82 纯文本输入框（无富文本/字数计数）
- 所有内容发布——商品描述、帖子正文、树洞发言、答疑提问——全是纯文本。无加粗、分段、列表。打字时不知道已打多少字，提交时超限才知道。

### #83 与校园系统零打通
- "郑轻校园"App 没有和学校任何系统打通。课表、成绩、校园卡余额都不在这里。二手、树洞、恋爱——任何社区 App 都能做，缺乏"郑轻"的不可替代性。

### #84 无分享功能
- 树洞帖子、商品详情页——无分享按钮、无复制链接、无生成分享图。内容传播靠截图发微信。

---

## AI 审核专题审计（24 项）

> 以下为 AI 内容审核系统的专项深度审计，部分与上述通用问题重叠，此处聚焦审核系统特有的技术漏洞和逻辑缺陷。

### P0 — AI 审核（8 项）

#### #85 审核失败时静默放行
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: API key 填错、API 地址挂了、网络不通 → `aiModerate` 所有错误被 catch 后返回 `'unknown'`。调用方（`afterCreate`/controller）将 `'unknown'` 等同于 `'safe'`，内容放行。用户和管理员完全不知道 AI 根本没在审。
- **根因**: `aiModerate` 的 catch 分支 `return 'unknown'` 与 `return 'safe'` 在调用侧无区分。熔断器打开后同样是静默放行。

#### #86 熔断器无健康上报/告警
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: 连续 3 次失败 → 熔断 5 分钟 → 半开测试。但熔断打开时无告警日志（仅 `logger.warn` 级别）、无管理员通知。管理员要等 5 分钟后发现还没恢复才能意识到。5 分钟内所有内容裸奔。

#### #87 编辑内容不重新过 AI
- **文件**: `goods/post/lostfound` controller 的 update 函数
- **现象**: 发布正常商品通过 L1+L2 → 编辑描述改成违规内容 → 只有路由层 `moderateBody`（L1 词表）。词表没命中的话内容永久留在线上。`updateGoods`/`updatePost`/`updateLostFound` 均未调 `afterCreate`。
- **根因**: 编辑 = AI 审核的天然绕过路径。

#### #88 并发创建 + 审核时序导致内容丢失审核
- **文件**: `qingtao-server/src/middleware/moderation.middleware.ts`
- **现象**: `afterCreate` 是 fire-and-forget。如果服务崩溃重启，还没执行完的 `backgroundCheck` 全部丢失——那些内容永久留在数据库里，没有任何后续审核补救。

#### #89 词表 JSON 加载失败无降级兜底
- **文件**: `qingtao-server/src/middleware/moderation.middleware.ts`
- **现象**: `sensitive-words.json` 被误删/改名/磁盘读取失败 → `fs.readFileSync` 抛异常 → 服务直接启动不起来。如果 JSON 格式损坏（手动编辑多了嵌套），`Object.values().flat()` 可能返回空数组——零词条，所有内容放行。

#### #90 500 字截断可被利用绕过 AI
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: `text.slice(0, 500)` 截断后发给 AI。攻击者在第 1-500 字写满正常内容，第 501 字开始放违规内容 → AI 审正常部分返回 safe → 全文放行。

#### #91 用户资料修改完全不经 AI
- **文件**: `qingtao-server/src/controllers/user.controller.ts`
- **现象**: 个人简介改成"代写论文加微信 XXXXX"。路由层 `moderateBody(['nickname', 'bio'])` 拦明显敏感词。但变体写法绕过词表后，`updateProfile` 无任何 AI 异步审核。个人资料全站可见——随每条评论、每篇帖子出现。展示面极广但审核覆盖为零。

#### #92 高危内容无即时阻断（自杀/恐怖威胁等）
- **文件**: `qingtao-server/src/services/moderation.service.ts`、审核中间件
- **现象**: 所有内容"先发布后审查"。如果学生发"我要跳楼"，AI 3 秒后检测到然后下架——但 3 秒内已公开展示。自杀倾向、恐怖威胁等极端内容应同步审核或至少即时标记。

### P1 — AI 审核（7 项）

#### #93 私聊消息审核结果不同步到接收方
- **文件**: `qingtao-server/src/middleware/moderation.middleware.ts`（`backgroundCheck`）
- **现象**: `backgroundCheck` 对 message 类型的处理是 `chatMessage.update({ content: '[该消息因违规已被屏蔽]' })`。但如果接收方已经打开聊天页面看到了原始内容，这个替换不会实时刷新。且前端对被替换为屏蔽文字的消息无特殊视觉标记。

#### #94 AI 审核被 afterCreate 内部的 L1 检查"吃掉"
- **文件**: `qingtao-server/src/middleware/moderation.middleware.ts`（`afterCreate` 函数）
- **现象**: `afterCreate` 内部：`if (!text || SAFE_PHRASES.has(text) || containsSensitive(text)) continue;` ——如果 `containsSensitive` 命中（词表匹配），跳过 AI。但请求已经过了路由层的 `moderateBody` 且没被拦，说明路由层正则没命中。如果两份词表不一致，可能出现：路由放行 → afterCreate 命中 → AI 跳过 → 内容裸奔。

#### #95 内容被审核下架后前端状态不一致
- **文件**: 各详情页轮询逻辑
- **现象**: 商品被 AI 下架 → `GoodsDetailPage` 轮询 4×3s 检测到 `offline` → 显示标记。但如果轮询结束 AI 还没审完，页面一直显示正常。恋爱帖子、树洞帖子被 AI 违规直接删除 → 用户回看 404，无任何解释。

#### #96 管理员无法查看 AI 审核记录/统计
- **文件**: 无审核日志模块
- **现象**: 无法知道"今天 AI 拦了多少内容、什么类型、准确率"。AI 审核对管理员完全不透明。误杀无法发现、无法恢复。用户被误删后无申诉渠道。

#### #97 申诉流程不存在（误判无救济途径）
- **文件**: 前端通知详情页、后端申诉 API
- **现象**: 用户收到"AI审核违规已下架"通知 → 点进去无"申诉"按钮。内容卡在"被下架但无人能恢复"的状态。管理员后台无内容管理面板 → 理论上可恢复但实际无法操作。

#### #98 树洞违规直接物理删除 → 误删不可逆
- **文件**: `qingtao-server/src/controllers/treehole.controller.ts`
- **现象**: 树洞 AI 审核发现违规 → `prisma.treeHolePost.delete()`（物理删除）。`treeHolePost` 表无 `status` 字段 → 无法软删除。一旦误判，内容永久丢失。

#### #99 同一用户连续违规无加重处理
- **文件**: 审核中间件
- **现象**: 用户 1 小时内连发 5 帖、3 条被 AI 判违规 → 每条独立处理（下架+通知）。无警告、无临时禁言、无封号阈值。可在 AI 延时窗口内连续发布违规内容。

### P2 — AI 审核（9 项）

#### #100 400 拦截消息暴露检测字段名
- **文件**: `qingtao-server/src/middleware/moderation.middleware.ts`
- **现象**: `moderateBody` 返回 `{ message: "title包含违规内容" }` → 暴露内部字段名（title/content）。攻击者可据此推断系统过滤规则。用户不理解为什么被拒，只知道"出 bug 了"。

#### #101 图片完全不被 AI 审核
- **文件**: 全系统
- **现象**: 整个 AI 审核仅对文字字段工作。用户上传的商品图、帖子图、树洞图、聊天图——完全不经过 AI。管理员后台图片审核靠人工逐张看。

#### #102 404 错误可能暴露审核状态
- **文件**: 各 GET detail controller
- **现象**: AI 下架返回 404、作者删除返回 404、ID 不存在返回 404——三种情况响应完全相同。如果未来错误提示变具体，这就成信息泄露通道（可推断哪些 ID 被 AI 下架）。

#### #103 AI prompt 与代码硬绑定 → 切换模型需改代码
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: system prompt 硬编码在 TS 文件中。换模型需要改 prompt → 改代码 → 重新部署。无环境变量或外部文件配置 prompt 的机制。

#### #104 AI 对非中文内容处理不明确
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: prompt 全中文指令，未说明"非中文内容按同样标准判断"。全英文违规内容发给中文 prompt 的模型 → 可能返回 `'0'`（安全）因为模型"看不懂"。代码仅判断 `'0'`/`'1'`，无语言检测。

#### #105 L1/L2 词表一致性结构脆弱
- **文件**: `moderation.middleware.ts`
- **现象**: `moderateBody` 直接用 `SENSITIVE_RE.test()`，`afterCreate` 用 `containsSensitive(text)`。虽然目前同一来源，但如果未来有人修改一处未同步另一处，会出现"路由放行 → afterCreate 二次检查命中 → AI 跳过"的路径。

#### #106 `afterCreate` 内部调用 `containsSensitive` 的循环代理
- **文件**: `moderation.middleware.ts` + `sensitive.ts`
- **现象**: `afterCreate` 定义在 `moderation.middleware.ts` 内，它调用的 `containsSensitive` 是同一文件导出的函数。而 `sensitive.ts` 又从 `moderation.middleware.ts` re-export 了 `containsSensitive`。逻辑上存在"同文件函数通过外部代理调用自己"的循环感。目前没问题，但架构脆弱。

#### #107 前端请求重试可绕过审核的时序
- **现象**: 敏感词加空格绕过 L1 → 内容到 controller → controller 内 `containsSensitive` 二次检查（词表代理）→ 如果也没命中 → 直接入库 + fire-and-forget AI。AI 审核本质是"先发布后审查"，无二次同步确认。

#### #108 AI 冷却期内新请求无处理策略
- **文件**: `qingtao-server/src/services/moderation.service.ts`
- **现象**: 熔断器 5 分钟冷却期内所有 AI 请求直接返回（不调用 API）。但返回什么？如果返回 `'unknown'` → 调用侧视为 safe → 冷却期内所有内容裸奔。没有"冷却期内暂缓发布等待人工审核"的队列或降级策略。

---

## 统计（含 AI 审核专题）

| 级别 | 通用 | AI 审核专题 | 合计 |
|------|------|------------|------|
| P0 — 安全/功能缺口 | 20 | 8 | **28** |
| P1 — 体验硬伤 | 33 | 7 | **40** |
| P2 — 优化 | 39 | 9 | **48** |
| **总计** | **92** | **24** | **116** |

---

## 本次会话已修复项

| 问题 | 状态 |
|------|------|
| 树洞路由挂载 `moderateBody` | ✅ 已修复 |
| `sensitive.ts` 代理统一到 `moderation.middleware.ts` | ✅ 已修复 |
| 树洞 L2 AI 异步审核 | ✅ 已修复 |
| 恋爱私信 L2 AI 异步审核 | ✅ 已修复 |
| 快捷短语白名单（免审） | ✅ 已修复 |

---

## 2026-06-17 漏洞修复批次

> 本批次聚焦 AI 审核安全闭环、设备绑定、多标签页同步、防抖等高价值修复。
> 修复前先系统验证了之前会话已修复的 25+ 项 P0/P1（恋爱通知/私信通知/Q&A审核/登录限流/AdminRoute守卫/文件魔术字节/商品所有权/树洞限流/忘记密码/注销账号/手机号绑定/购物车去重/收藏通知清理/通知一键已读/私聊发图/AI熔断告警/编辑重新过AI/用户资料AI审核/违规用户限制/版本号更新/编辑图片回显等），确认均已在代码中实现。

### 本次新增修复

| # | 问题 | 文件 | 修复方案 |
|---|------|------|----------|
| #90 | AI 审核 500 字截断可被利用绕过 | `services/moderation.service.ts` | 新增 `sliceForAI()`：文本超 1800 字时审核"前 900 字 + [省略] + 后 900 字"拼接，防止违规内容藏在截断之外 |
| #94 | `afterCreate` 内部 `containsSensitive` 命中跳过 AI → 内容裸奔 | `middleware/moderation.middleware.ts` | 命中敏感词时直接下架 + 通知（不再 continue 跳过），堵住"路由放行→afterCreate命中→AI跳过"漏洞路径 |
| #13 | Token 无设备绑定、可被复制 | `middleware/auth.ts` | 新增 `computeRequestFingerprint()`（UA + IP 网段 hash），authMiddleware 校验 payload.fp 与当前请求 fp，不匹配触发告警 |
| #14 | 跨设备同时登录无感知 | `middleware/auth.ts` | fp 不匹配时异步通知用户"账号登录环境异常"（1 小时去重），实现跨设备登录有感知 |
| #44 | 多标签页状态不同步 | `stores/authStore.ts` + `utils/storage.ts` | 监听 window 'storage' 事件，其他标签页登出/登录时同步更新当前页 auth 状态；导出 TOKEN_KEY/USER_KEY 常量 |
| #30 | 评论/点赞按钮无防抖（连点重复提交） | `pages/explore/NoteDetailPage.tsx` | handleLike 加 `likingRef` 防并发；handleComment 加 `commenting` state + 按钮 disabled |

### 验证结果

- 后端 `tsc --noEmit`：零错误 ✅
- 前端 `tsc --noEmit`（修改文件）：authStore.ts / storage.ts 零错误 ✅；NoteDetailPage.tsx 仅剩1个预存的 UserAvatar `size="xs"` 类型错误（line 214，与本次防抖修改无关）
- 设备指纹基础设施复用：`auth.service.ts` 的 `generateTokens` 早已支持 fingerprint 参数，本次补齐了 authMiddleware 的校验闭环

### 备注

- Notification.type 为 String 类型（非 enum），新增的 `security_alert` 类型无需数据库迁移
- 设备指纹校验采用"告警不踢出"策略，避免误伤移动网络切换/同浏览器跨网段场景；如需更严格可在 `notifyDeviceMismatch` 后追加 `return unauthorized(res)`
