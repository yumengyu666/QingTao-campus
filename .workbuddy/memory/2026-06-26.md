## 2026-06-26

### API Key 更新 + 项目启动
- Agent 自动对话"小轻助手"不能正常使用，原因：`.env` 中旧 Key `sk-5bcf6058074b4e5eb80d63721d71aec0` 已失效
- 更新为新 Key `sk-0e3d9ed1a49540f88888fbdb65633230`（DeepSeek V4 API，模型 `deepseek-v4-flash`）
- 验证：新 Key 通过 `curl` 测试，DeepSeek API 正常返回
- 后端启动：`qingtao-server` 运行在 `http://localhost:3000`（`tsx src/index.ts`）
- 前端启动：`qing-tao-campus` Vite 运行在 `http://localhost:5174`（端口被占自动切换）
- 旧 Node 进程 (PID 38092) 占 3000 端口需先 kill 再重启
- API Key 存储在 `qingtao-server/.env` 的 `MODERATION_API_KEY`，同时服务 Agent 对话和 AI 内容审核
