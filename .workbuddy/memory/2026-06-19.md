# 2026-06-19 液态玻璃版本 BUG 修复

## 修复概览
用户反馈液态玻璃版本（/lg 路由）很多页面打不开，全面诊断并修复了 4 类共 17 处问题。

## 构建阻断级修复（P0）

### 1. LiquidTabBar.tsx 第22行 `\`n` 转义序列
- **问题**: 注释和代码之间使用字面反引号n而非实际换行，导致 TS 编译失败
- **修复**: 替换为实际换行符
- **文件**: `src/components/layout/LiquidTabBar.tsx`

### 2. LiquidGlassDemo.tsx 第559行 `\`n` 转义序列
- **问题**: useState语句之间使用字面反引号n，导致级联编译错误
- **修复**: 替换为实际换行符
- **文件**: `src/pages/LiquidGlassDemo.tsx`

## CSS 修复

### 3. liquid-glass.css 孤立 CSS 变量（第603-609行）
- **问题**: 6 个 CSS 自定义属性声明在无选择器上下文中（已在 .liquid-glass 内有重复声明）
- **修复**: 删除冗余声明
- **文件**: `src/styles/liquid-glass.css`

### 4. LiquidGlassLayout.tsx 冗余 key prop（第316行）
- **问题**: 外层 key={location.pathname} 与 PageTransition 内部 key 重复
- **修复**: 移除外层冗余 key
- **文件**: `src/components/layout/LiquidGlassLayout.tsx`

## 导航缺失 /lg 前缀（P0 运行时问题）

### 5. SearchPage.tsx — 6处
- **问题**: 搜索建议/结果点击使用原始 navigate 而非 useAppNavigate，在 /lg 下跳出液态玻璃环境
- **修复**: 全部改为 nav() (行 102, 107, 108, 109, 110, 111)
- **文件**: `src/pages/search/SearchPage.tsx`

### 6. ChatPage.tsx — 3处 + NaN 风险
- **问题**: 点击用户头像、聊天设置、名片卡片使用原始 navigate；parseInt(userId!) 可能为 NaN
- **修复**: 改为 nav() (行 521, 559, 652)；NaN 修复 (行 466, 483)
- **文件**: `src/pages/chat/ChatPage.tsx`

### 7. NoteDetailPage.tsx — 2处
- **问题**: 缺失 useAppNavigate 导入，两处 navigate 调用无 /lg 前缀
- **修复**: 添加导入 + 改为 nav()
- **文件**: `src/pages/explore/NoteDetailPage.tsx`

### 8. CollectionDetailPage.tsx — 1处
- **问题**: 缺失 useAppNavigate 导入
- **修复**: 添加导入 + 改为 nav()
- **文件**: `src/pages/explore/CollectionDetailPage.tsx`

### 9. DatingChatPage.tsx — 1处
- **修复**: navigate → nav
- **文件**: `src/pages/dating/DatingChatPage.tsx`

## 渲染崩溃修复（P1）

### 10. UserProfilePage.tsx 评分星级宽度计算
- **问题**: `reviews.list.Math.max(Math.max(filter(...)))` — 完全错误的链式调用，会直接导致渲染崩溃
- **修复**: 重写为 `Math.max(reviews.list.filter(...).length / ... * 100, 3)`
- **文件**: `src/pages/profile/UserProfilePage.tsx`

## 类型安全修复

### 11. EmptyState.tsx Framer Motion ease 类型
- **修复**: `ease: [0.16, 1, 0.3, 1]` → `ease: [0.16, 1, 0.3, 1] as const`（2处）
- **文件**: `src/components/common/EmptyState.tsx`

### 12. HomePage.tsx Framer Motion ease 类型
- **修复**: 同上
- **文件**: `src/pages/home/HomePage.tsx`

### 13. TradeIntentsPage.tsx 重复属性
- **修复**: 删除重复的 `rejected` 对象属性
- **文件**: `src/pages/trade/TradeIntentsPage.tsx`

## 验证结果
- ✅ `npx vite build` — 构建成功，848ms
- ✅ 所有 53 条 /lg 路由对应页面均可正常构建
- ⚠️ 项目仍有约 100+ 预存 TS 类型警告（非阻断，预存问题）
