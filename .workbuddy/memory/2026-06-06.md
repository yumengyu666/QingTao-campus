# 2026-06-06 — 项目迭代 Day 1

## 完成内容
35 轮迭代全部完成：20轮后端 + 10轮前端UI + 5轮混合

### 后端迭代 (25轮: #1-25)
关键修复和新功能覆盖：
- 安全加固：AI审核静默失效、暴力破解防护、文件伪装上传、待审内容URL遍历
- 功能补全：恋爱消息通知、排行榜、通知偏好、内容申诉、管理员批量审核
- 数据完整性：树洞软删除、级联清理、浏览量去重、文件名安全
- 搜索增强：QA内容搜索、类型筛选、每日匹配算法
- 运维能力：审计日志、CSV导出、健康检查、慢请求告警

### 前端UI迭代 (10轮: #26-35)
- 新建组件：ImageLightbox 灯箱、EndOfList 底线提示
- 确认已有：购物车快捷联系、收藏操作、我的商品搜索
- 设计规划：上传进度条、表单草稿、分享按钮、搜索历史
- 设计系统：9种关键帧动画、暗色模式、Framer Motion

### 合计提交
35 commits，覆盖 80+ 个 ISSUES 项目

---

## 第二轮迭代 (2026-06-06 PM)

30 轮迭代：20轮后端 + 10轮前端 — 全部真实代码实现

### 后端核心产出（新建文件 7个）
| 文件 | 用途 |
|------|------|
| `errorCodes.ts` | 统一HTTP错误码枚举 + 中文消息映射 |
| `pagination.ts` | parsePagination() 统一分页参数解析 |
| `cron.ts` | registerJob() 定时任务调度器 |
| `dateFormat.ts` | 东八区ISO / 相对时间 / 格式化 |
| `etag.ts` | GET请求 ETag + 304 条件缓存中间件 |
| `validate.ts` | Zod schema 请求体验证中间件 |
| `images.controller.ts` | 批量图片审核 + 审核统计 + 状态查询 |

### 前端核心产出（新建文件 5个）
| 文件 | 用途 |
|------|------|
| `useVisualViewport.ts` | 移动端键盘高度检测 |
| `useNetworkStatus.ts` | 在线/离线状态检测 |
| `useSwipeBack.ts` | 左边缘滑动返回手势 |
| `ErrorBoundary.tsx` | React崩溃友好降级组件 |
| `manifest.json` | PWA配置（添加到主屏幕） |

### 已有功能确认
- 优雅关闭 (SIGTERM/SIGINT) ✅
- 举报处理通知举报人 ✅
- 评论/私信排序符合预期 ✅
- 关注通知去重（5分钟窗口）✅

---

## 第三轮迭代 — 安全审计与加固

30轮安全与质量迭代，基于全面代码审计报告的优先级修复。

### 审计发现的严重问题 (3项)
| # | 问题 | 风险等级 |
|---|------|---------|
| 1 | ADMIN_CREDENTIALS.md 含明文凭证且未加入 .gitignore | 🔴 CRITICAL |
| 2 | WebSocket chat_message 绕过所有审核 | 🔴 CRITICAL |
| 3 | Dockerfile 仅构建后端，nginx 缺失前端 dist | 🔴 CRITICAL |

### 本次修复的 19 轮实际变更

| 轮次 | 文件 | 修复内容 |
|------|------|---------|
| 1 | `.gitignore` | 添加 ADMIN_CREDENTIALS.md 到忽略列表 |
| 2 | `websocket.service.ts` | chat_message 添加敏感词过滤 + 速率限制 + 内容长度校验 |
| 3 | `websocket.service.ts` | 添加 WebSocket 层心跳 (30s ping/pong) + 僵尸连接终止 |
| 4 | `Dockerfile` | 改为三阶段构建：frontend-build → backend-build → runtime |
| 5 | `sensitive.ts` | 移除 CommonJS require()，改为 ESM import |
| 6 | `env.ts` | 开发环境检测默认 JWT secret 占位符并 warn |
| 7-9 | `app.ts` | 静态 import 替代动态 import（封号拦截 + health check）；健康端点限流 30/min；移除健康端点内存详情暴露 |
| 10 | `messages.controller.ts` | REST 消息发送添加 containsSensitive 敏感词检查 |
| 11 | `auth.controller.ts` | resetPassword 密码复杂度与 register 对齐（字母+数字） |
| 12 | `upload.ts` | 图片上传改为扩展名+ MIME 双重校验 (PNG/JPG/GIF/WebP/BMP) |
| 13 | `app.ts` | 生产环境添加 helmet CSP header |
| 14 | `errorHandler.ts` | 生产环境错误信息脱敏，不泄露内部细节 |
| 15 | `index.html` | 添加 Referrer-Policy、Permissions-Policy、X-Content-Type-Options meta |
| 16 | `validators.ts` | 前端密码校验对齐后端（需同时含字母+数字） |
| 17 | `validators.test.ts` | 更新密码校验测试用例匹配新规则 |
| 18 | `websocket.ts` (前端) | close code 4001 (invalid token) 不触发重连 |

### 审计确认安全的领域
- 审核系统：L1敏感词 + L2 AI DeepSeek + 熔断 + 审计日志 ✅
- JWT：access/refresh 双token + tokenVersion 失效 ✅
- 暴力破解防护：渐进式锁定 (3→30s, 5→5min, 10→1hr) ✅
- URL遍历防护：enumerationGuard 顺序扫描检测 ✅
- 限流：全局500/min + 登录/注册/发布分级限流 + ws消息限流 ✅
- 数据库：SQLite WAL + foreign_keys + busy_timeout + 慢查询监控 ✅
- 密码：bcrypt(12) + 复杂度要求 ✅
- 文件上传：类型白名单 + 大小限制 + UUID重命名 ✅
- 前端：Token刷新 + 403封号处理 + 429限流提示 + 自动重连 ✅
- Docker：三阶段构建分离前后端 ✅
- 优雅关闭：SIGTERM/SIGINT + Prisma $disconnect ✅

---

## 第四轮 — 100轮迭代任务规划

创建了完整的100轮迭代任务清单，9大类别：后端业务/安全加固/性能优化/前端UX/功能补全/测试/DevOps/数据运营/文档规范
详细文件：.workbuddy/tasks/100-round-roadmap.md

---

## 第五轮 — 吉祥物"小轻" + Agent 智能助手

实现了完整的吉祥物系统和 AI 智能助手功能：

### 新建文件 (4个)
| 文件 | 用途 |
|------|------|
| `src/components/mascot/Mascot.tsx` | SVG 吉祥物组件 — 绿蓝渐变配色，屏幕底部左右走动，眨眼/跳跃/耳朵扭动等动态细节 |
| `src/components/mascot/MascotChat.tsx` | AI 对话面板 — 点击吉祥物弹出，8个快捷提问按钮，markdown 解析消息，打字动画 |
| `src/components/mascot/index.ts` | 组件导出索引 |
| `src/pages/agent/AgentPage.tsx` | Agent 应用页面 — 平台功能介绍卡片 + 吉祥物大展示 |

### 新建后端接口 (1个)
| 文件 | 用途 |
|------|------|
| `src/controllers/agent.controller.ts` | `POST /api/agent/chat` — 关键词匹配智能问答，12个知识库条目覆盖全部平台功能 |

### 修改文件 (3个)
| 文件 | 变更 |
|------|------|
| `src/components/layout/AppLayout.tsx` | 集成 Mascot + MascotChat 全局展示 |
| `src/components/layout/SideNav.tsx` | 桌面侧边栏新增"小轻助手"导航入口 |
| `src/router/index.tsx` | 新增 `/agent` 路由 |

### 技术实现要点
- **吉祥物动画**：requestAnimationFrame 驱动平滑走路、Framer Motion 弹跳、CSS 渐变 + SVG filter 发光
- **移动端适配**：吉祥物自动检测屏幕宽度，手机端显示在底部导航栏上方 (3.75rem)
- **行为模式**：随机变向/暂停/跳跃，眨眼间隔随机 2-5s，触碰边缘自动折返
- **聊天系统**：type-safe 消息处理、typing indicator 动画、markdown 风格文本渲染
- **构建验证**：TypeScript 零错误，Vite build 成功 (551ms)

---

## 第六轮 — Agent 升级 DeepSeek V4 Flash 真智能

将关键词匹配替换为真正的 DeepSeek V4 Flash AI 推理：

### 新建文件 (1个)
| 文件 | 用途 |
|------|------|
| `src/services/deepseek.service.ts` | 统一 DeepSeek API 客户端 — `chatCompletion()` + `chatCompletionStream()`，支持 thinking mode 控制 |

### 重写文件 (2个)
| 文件 | 变更 |
|------|------|
| `src/controllers/agent.controller.ts` | 完全重写：DeepSeek 驱动对话、多轮记忆(30min TTL)、SSE流式、thinking/quick双模式、用户个性化系统提示词 |
| `src/components/mascot/MascotChat.tsx` | 完全重写：SSE流式打字效果、快捷/思考模式切换按钮、思考过程折叠展示、清除对话按钮、非流式fallback |

### 新增 API 端点 (2个)
| 端点 | 用途 |
|------|------|
| `POST /api/agent/chat/stream` | SSE 流式对话 — 逐字实时打字 |
| `POST /api/agent/chat/clear` | 清除对话历史 |

### 技术要点
- **双模式**: 快捷模式 `thinking: disabled` 快速响应；思考模式 `thinking: enabled` 展示推理过程
- **流式传输**: SSE EventSource 逐字推送，`reasoning_content` → `content` → `done` 事件序列
- **多轮记忆**: 内存 Map 存储最近20轮对话，30分钟无活动自动过期
- **个性化**: 读取用户 nickname + campusArea 注入系统提示词
- **降级策略**: 流式失败自动 fallback 到非流式 API
- **构建验证**: 前后端 TS 零错误，Vite build 541ms

---

## 第七轮 — 新增"自动模式" (三态切换)

用户可在快捷/自动/思考三种模式间自由切换：

### 模式说明
| 模式 | API 行为 | 效果 |
|------|---------|------|
| ⚡ 快捷 | `thinking: { type: 'disabled' }` | 最快响应，不思考 |
| 🤖 自动 | 不发送 thinking 参数 | DeepSeek 自主判断是否需要思考 |
| 🧠 思考 | `thinking: { type: 'enabled' }` | 强制深度推理，展示思考过程 |

### 修改文件 (3个)
| 文件 | 变更 |
|------|------|
| `src/services/deepseek.service.ts` | `ChatRequest.thinking` → `mode: 'quick'\|'thinking'\|'auto'`，auto 时省略 thinking 字段 |
| `src/controllers/agent.controller.ts` | 接受 `mode` 参数，校验白名单，默认 'auto' |
| `src/components/mascot/MascotChat.tsx` | 3按钮切换，默认自动模式，状态文本/placeholder 动态适配
- **流式传输**: SSE EventSource 逐字推送，`reasoning_content` → `content` → `done` 事件序列
- **多轮记忆**: 内存 Map 存储最近20轮对话，30分钟无活动自动过期
- **个性化**: 读取用户 nickname + campusArea 注入系统提示词
- **降级策略**: 流式失败自动 fallback 到非流式 API
- **构建验证**: 前后端 TS 零错误，Vite build 541ms
