# 前端页面完善计划（详细版）

> 每个页面标注：当前状态 → 目标状态 → 需要的后端接口 → UI 布局 → 交互细节 → 边界状态

---

## 1. LostFoundDetailPage（重写）

**文件**: `src/pages/square/LostFoundDetailPage.tsx`
**路由**: `/square/lostfound/:id`
**当前状态**: ❌ 全部硬编码假数据，不调用任何 API
**目标状态**: ✅ 完整的失物招领详情页

### 需要的后端接口
```
GET    /api/lostfound/:id              → { code, data: LostFoundItem }
GET    /api/lostfound/:id/comments     → { code, data: { list, total, page, pageSize } }
POST   /api/lostfound/:id/comments     ← { content } → { code, data: comment }
DELETE /api/lostfound/:id/comments/:cid → { code, message }
PATCH  /api/lostfound/:id/resolve      → { code, message }
DELETE /api/lostfound/:id              → { code, message }
POST   /api/reports                    ← { targetType:"lostfound", targetId, reason }
```

### 页面布局
```
┌──────────────────────────────┐
│  Header（返回 + 分享）        │
├──────────────────────────────┤
│  类型标签 + 校区标签 + 悬赏    │
│  标题                        │
│  作者（头像+昵称→可点击进主页） │
│  时间 · 浏览量 · 举报 · 私信   │
│  ───────────────────────     │
│  📍 位置  🕐 丢失时间         │
│  详细描述（保留换行）          │
│  图片（如有）                 │
├──────────────────────────────┤
│  联系方式                    │
│  微信：xxx  [复制]            │
│  QQ：xxx   [复制]             │
├──────────────────────────────┤
│  回复（N）                   │
│  ├ 头像 昵称 时间            │
│  ├ 评论内容                  │
│  └ [删除]（仅自己的评论）     │
├──────────────────────────────┤
│  [输入框]  [发送]  ← 底部固定 │
│  （作者操作区）               │
│  [标记已解决] [删除]          │
└──────────────────────────────┘
```

### 交互细节
- 页面加载 → Skeleton 骨架屏
- 数据为空/404 → 空状态提示
- 点击作者头像/昵称 → 跳转 `/user/:id`
- 点击"私信" → 跳转 `/messages/:userId`
- 复制微信/QQ → toast 提示 + 图标临时切换为 ✓
- 举报 → 弹出举报弹窗（选择原因 → 提交）
- 评论输入 → Enter 发送，空内容禁用按钮
- 自己的评论 → 显示删除按钮，点击弹出确认
- 作者本人 → 显示"标记已解决"和"删除"按钮
- 标记已解决 → 即时更新 UI 状态
- 移动端：输入框固定在键盘上方（visualViewport）

### 边界状态
| 状态 | 处理 |
|------|------|
| 加载中 | Skeleton 骨架屏 |
| 商品不存在/404 | "该失物信息不存在或已被删除" |
| 网络错误 | toast "网络错误，请检查后端服务" |
| 评论为空 | "暂无回复，来提供线索吧" |
| 已解决 | 类型标签旁显示 ✅ 已解决 |

---

## 2. QaDetailPage（增强）

**文件**: `src/pages/qa/QaDetailPage.tsx`
**路由**: `/qa/:id`
**当前状态**: ⚠️ 基本功能有，缺少骨架屏、空状态、动画细节
**目标状态**: ✅ 完整的答疑详情页

### 需要的后端接口
```
GET    /api/qa/:id                    → { code, data: { post, answers } }
POST   /api/qa/:id/answers            ← { content, images } → { code, data: answer }
POST   /api/qa/answers/:id/vote       → { code, message }
POST   /api/qa/answers/:id/best       → { code, message }
```

### 页面布局
```
┌──────────────────────────────┐
│  Header（返回）               │
├──────────────────────────────┤
│  ❓ 求助 / 💡 分享 标签       │
│  [已解决] 标签（条件显示）     │
│  标题                        │
│  内容（保留换行）              │
│  图片（如有，点击放大）        │
│  作者 · 时间 · 浏览量          │
├──────────────────────────────┤
│  N 个回答                    │
│  ┌──────────────────────┐    │
│  │ 🟢 最佳答案（绿色边框） │    │
│  │ 头像 昵称 时间  ✓最佳  │    │
│  │ 回答内容               │    │
│  │ 图片（如有）            │    │
│  │ [👍 N] [采纳]          │    │
│  └──────────────────────┘    │
│  ┌ 普通答案 ─────────────┐   │
│  │ 头像 昵称 时间         │   │
│  │ 回答内容               │   │
│  │ [👍 N]                │   │
│  └──────────────────────┘    │
├──────────────────────────────┤
│  [输入框]  [发送]  ← 底部固定 │
└──────────────────────────────┘
```

### 交互细节
- 加载 → Skeleton（标题+内容+回答列表）
- 空回答 → "暂无回答，来写第一个吧" + 动画箭头指向输入框
- 点赞 → 数字即时 +1/-1，图标变色，按钮短暂放大
- 采纳 → 回答卡片出现绿色边框动画，图标变为 ✓
- 采纳后 → 问题标题旁显示"已解决"，所有回答的"采纳"按钮消失
- 仅问题作者可看到"采纳"按钮
- 回答按：最佳答案置顶 → 赞数降序

### 边界状态
| 状态 | 处理 |
|------|------|
| 加载中 | Skeleton（标题骨架 + 3 行回答骨架） |
| 问题不存在 | "该问题不存在或已被删除" |
| 回答为空 | 引导文案 + 动画 |
| 已解决 | 绿色标签 + 最佳答案高亮 |
| 自己不能采纳自己的回答 | "采纳"按钮不显示在作者自己的回答上 |

---

## 3. QaListPage + 提问功能（增强）

**文件**: `src/pages/qa/QaListPage.tsx`
**路由**: `/qa`
**当前状态**: ⚠️ 列表正常，FAB 跳转到不存在的 `/qa/new`
**目标状态**: ✅ 带提问弹窗的列表页

### 需要的后端接口
```
GET    /api/qa                         ?category=&page= → { code, data: { list, total } }
POST   /api/qa                         ← { title, content, category, type, images }
POST   /api/upload/image               ← FormData → { urls }
```

### 新增：提问弹窗 (AskQuestionModal)
```
┌──────────────────────────────┐
│  发布问题              [✕]   │
├──────────────────────────────┤
│  类型: [❓求助] [💡分享]     │
│  分类: 下拉选择              │
│  标题: [______________]      │
│  内容: [______________]      │
│        (最多2000字)          │
│  图片: [ImageUploader]       │
│                              │
│  [取消]      [发布]          │
└──────────────────────────────┘
```

### 交互细节
- FAB 点击 → 弹出提问弹窗（从底部滑入）
- 标题必填，内容选填
- 图片最多 9 张
- 发布按钮 → loading → toast 成功 → 关闭弹窗 → 刷新列表
- 弹窗背景点击不关闭（防止误触丢失内容）

---

## 4. PostDetailPage（增强）

**文件**: `src/pages/square/PostDetailPage.tsx`
**路由**: `/square/post/:id`
**当前状态**: ⚠️ 举报按钮未接入
**目标状态**: ✅ 完整的帖子详情页

### 需要新增
```
POST   /api/reports                    ← { targetType:"post", targetId, reason }
```

### 新增：举报弹窗 (ReportModal)
```
┌──────────────────────────────┐
│  举报帖子              [✕]   │
├──────────────────────────────┤
│  请选择举报原因：             │
│  ○ 垃圾广告                  │
│  ○ 不实信息                  │
│  ○ 人身攻击                  │
│  ○ 色情低俗                  │
│  ○ 违法违规                  │
│  ○ 其他（可填写）             │
│  [____________]              │
│                              │
│  [取消]      [提交举报]       │
└──────────────────────────────┘
```

### 交互细节
- 点击"举报"→ 弹出举报弹窗
- 选择原因 → 提交 → toast "举报已提交"
- 不可重复举报（前端暂不做限制）

---

## 5. DatingProfilePage（增强）

**文件**: `src/pages/dating/DatingProfilePage.tsx`
**路由**: `/dating/profile`
**当前状态**: ⚠️ 基本功能有，缺加载态和校验
**目标状态**: ✅ 完整的恋爱资料编辑页

### 增强内容
- 页面加载 → Skeleton 骨架屏（头像圆 + 3 行输入框）
- 昵称必填校验（不能为空，最多 12 字）
- 头像上传 → 即时预览 + 上传中 spinner
- 保存 → loading 态 → toast → 返回上一页
- 返回时如果已修改未保存 → 提示"放弃修改？"（可选）

---

## 6. DatingSquarePage（增强）

**文件**: `src/pages/dating/DatingSquarePage.tsx`
**路由**: `/dating`
**当前状态**: ⚠️ 发帖弹窗无图片上传
**目标状态**: ✅ 完整的恋爱广场

### 增强内容
- 发帖弹窗增加 ImageUploader（最多 9 张）
- 关注按钮 → 即时更新 UI（乐观更新）
- 帖子列表 → 下拉刷新（移动端）
- 图片点击放大

---

## 7. 图片审核页（新增，替代旧的 AdminReviewPage）

**文件**: `src/pages/admin/AdminImagesPage.tsx`
**路由**: `/admin/images`
**当前状态**: ❌ 不存在
**目标状态**: ✅ 管理员图片审核工作台

### 需要的后端接口
```
GET    /api/admin/images              ?status=pending&page= → { list, total, page }
POST   /api/admin/images/:id/approve   → 图片变清晰
POST   /api/admin/images/:id/reject    ← { reason } → 图片拒绝 + 关联内容下架 + 通知作者
```

### 页面布局
```
┌──────────────────────────────────┐
│  Header（图片审核）               │
├──────────────────────────────────┤
│  筛选: [全部] [待审核 N] [已通过] [已拒绝] │
├──────────────────────────────────┤
│  ┌ 审核卡片 ─────────────────┐   │
│  │ [模糊图] [清晰图（点击放大）] │   │
│  │ 上传者: 张三               │   │
│  │ 来源: 商品 — "iPhone 15"   │   │
│  │ 上传时间: 3 分钟前         │   │
│  │ [通过] [拒绝]              │   │
│  └──────────────────────────┘   │
│  ┌ 审核卡片 ─────────────────┐   │
│  │ ...                       │   │
│  └──────────────────────────┘   │
├──────────────────────────────────┤
│  分页                           │
└──────────────────────────────────┘
```

### 交互细节
- 默认显示"待审核"
- 点击清晰图 → 全屏预览
- 通过 → 即时从队列移除
- 拒绝 → 弹出原因输入框 → 提交 → 内容自动下架 + 通知作者
- 已审核记录保留在"已通过/已拒绝"标签中可查看

---

## 8. 树洞页面（新增）

**文件**: `src/pages/treehole/TreeHolePage.tsx`
**路由**: `/treehole`
**目标状态**: ✅ 匿名树洞

### 后端接口（待实现）
```
GET    /api/treehole                   → { list, total, page }
POST   /api/treehole                   ← { content, images } → 自动生成匿名 code
GET    /api/treehole/:id               → { post, comments }
POST   /api/treehole/:id/comments      ← { content }
POST   /api/treehole/:id/like          → 点赞
```

### 页面布局
```
┌──────────────────────────────┐
│  Header（树洞）               │
├──────────────────────────────┤
│  ┌──────────────────────┐    │
│  │ 💬 说点什么...  [发布] │    │
│  └──────────────────────┘    │
│                              │
│  ┌ 匿名帖 ───────────────┐   │
│  │ 🎭 匿名者#A3F2        │   │
│  │ 内容...               │   │
│  │ ❤️ 12  💬 3           │   │
│  └──────────────────────┘    │
│  ┌ 匿名帖 ───────────────┐   │
│  │ 🎭 匿名者#B7E1        │   │
│  │ 内容...               │   │
│  │ ❤️ 8  💬 1            │   │
│  └──────────────────────┘    │
└──────────────────────────────┘
```

### 交互细节
- 无需登录（可选：登录后保留匿名 code）
- 匿名 code 格式：`匿名者#XXXX`（4 位随机 hex）
- 点赞 → 即时 +1
- 点击帖子 → 展开评论（内联展开，不跳转新页面）
- 评论也使用相同的匿名 code

---

## 9. 树洞评论内联展开

点击树洞帖子后，卡片下方展开评论区：
```
┌ 匿名帖 ─────────────────────┐
│ 🎭 匿名者#A3F2              │
│ 今天在图书馆看到一个同学...    │
│ ❤️ 12  💬 3                 │
├ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤
│ 🎭 匿名者#C8D4: 我也看到了!  │
│ 🎭 匿名者#E2F9: 哈哈好巧     │
│ 🎭 匿名者#A1B2: 在几楼啊?    │
│ [写下评论...]      [发送]    │
└──────────────────────────────┘
```

---

## 10. 考试资料页面（新增）

**文件**: `src/pages/resources/ResourceListPage.tsx`
**路由**: `/resources`
**目标状态**: ✅ 考试资料列表 + 上传

### 后端接口（待实现）
```
GET    /api/resources                 ?courseName=&type=&page=
GET    /api/resources/:id             → 详情 (+下载量)
POST   /api/resources                 ← { courseName, courseCode, title, type, fileUrl, description }
POST   /api/resources/:id/download    → 下载量+1
DELETE /api/resources/:id
```

### 页面布局
```
┌──────────────────────────────┐
│  Header（考试资料）           │
├──────────────────────────────┤
│  [搜索课程...]                │
│  分类: [全部] [试卷] [笔记]   │
│       [思维导图] [实验报告]    │
│       [其他]                  │
├──────────────────────────────┤
│  ┌ 资料卡片 ─────────────┐   │
│  │ 📄 高等数学期末试卷     │   │
│  │ MATH101 · 试卷         │   │
│  │ ⬇ 23 次下载            │   │
│  └──────────────────────┘    │
│  ┌ 资料卡片 ─────────────┐   │
│  │ 📝 数据结构笔记         │   │
│  │ CS201 · 笔记            │   │
│  │ ⬇ 15 次下载            │   │
│  └──────────────────────┘    │
├──────────────────────────────┤
│  FAB: [+] 上传资料           │
└──────────────────────────────┘
```

---

## 11. 全局交互统一

### 键盘适配
所有带底部输入框的页面统一使用 `visualViewport` API：
- ChatPage（已实现）
- LostFoundDetailPage
- QaDetailPage
- PostDetailPage
- GoodsDetailPage

### 图片预览
所有图片点击放大功能统一抽取为公共逻辑：
- 聊天图片（已实现）
- 帖子图片
- 商品图片
- 失物图片

### 确认弹窗
删除操作统一使用确认：
- `confirm('确定删除吗？')` — 简单场景
- 自定义 ConfirmModal — 重要场景

### Toast 规范
- 成功：`toast.success('xxx')`
- 失败：`toast.error('xxx')`
- 网络错误：`toast.error('网络错误，请检查后端服务')`

### 空状态规范
所有空状态使用 `EmptyState` 组件：
```
message: 主提示文字
description: 辅助说明文字
icon: 自定义图标
action: 操作按钮（可选）
```

### 加载规范
所有页面加载使用 `Skeleton` 组件，不再出现"加载中..."文字：
- 列表页 → `Skeleton.List` 或 `Skeleton.Grid`
- 详情页 → `Skeleton.Detail`
- 会话页 → `Skeleton.Conversation`
