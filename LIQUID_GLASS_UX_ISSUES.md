# 液态玻璃版本 · 用户体感问题 100 条

> 审查范围：`qing-tao-campus/src` 液态玻璃相关全部页面和组件  
> 审查视角：纯用户操作路径 —— 点击、滚动、等待、阅读、输入、返回  
> 审查日期：2026-06-18

---

## 一、导航迷失 · 用户找不到想要的（1–14）

**1. 底部 Tab "发布" 点击后跳转的是问答页而非发布页** — 在 `LiquidGlassDemo` 中，发布按钮的 onClick 逻辑是 `setPage(tab.key === "publish" ? "qa" : tab.key)`，用户点"发布"却跳到了"答疑专区"，完全偏离预期。`LiquidGlassDemo.tsx:472`

**2. "广场"Tab 图标是一个圆形+感叹号** — 视觉上像"警告/信息"而非"探索/发现"，新用户扫一眼 TabBar 无法认知到这是广场入口。`LiquidTabBar.tsx:18-22`

**3. 液态玻璃和多达 40+ 条路由全在 `/lg/*` 下，URL 中包含 `/lg/` 前缀** — 用户分享链接给朋友时出现 `/lg/goods/123`，接收方若没用过液态玻璃版会被直接带入陌生布局，没有引导提示。

**4. 从液态玻璃子页面返回时没有保存浏览位置** — `LiquidGlassLayout` 中 `useEffect(() => { window.scrollTo({ top: 0 }); }, [pathname])` 每次路由变化都滚回顶部，但滚动容器是 `main#lg-main` 而不是 `window`，结果是既没滚回顶部，又丢失了之前的滚动位置。`LiquidGlassLayout.tsx:95-96`

**5. 搜索框是一个 `<button>` 而非 `<input>`，用户无法直接键入** — `SearchBar` 组件渲染成一个点击跳转按钮，用户以为可以直接打字却发现必须点击进入新页面才能搜索，多一步操作。`SearchBar.tsx:21-31`

**6. 主 Tab 页面和子页面的 header 结构不对称** — 主页面 header 左边是 logo 右边是搜索+暗色切换，子页面 header 左边是返回按钮+标题，右边是一个空的 9px 占位 div，在两个页面之间切换时顶部布局突变。`LiquidGlassLayout.tsx:158-188`

**7. 从商品详情页退回到列表页时，之前选择的筛选条件全部丢失** — `GoodsListPage` 的筛选状态只存在于组件 state 中，离开即销毁，用户每次都要重新筛选。

**8. "消息"页面存在两个入口但行为不同** — `ConversationsPage` 中 AI 助手卡片始终固定在列表顶部，但在液态玻璃 `LiquidGlassDemo` 的 `MessagesPage` 中 AI 助手只是一个普通列表项，用户从两个入口进入看到的结构不一致。`ConversationsPage.tsx:75-92`

**9. "恋爱空间"模块的提示条 `"广场关注和恋爱关注是独立的"` 位于页面顶部** — 用户读完这个提示后，下方帖子列表中的"关注"按钮究竟是"广场关注"还是"恋爱关注"仍然混淆。`DatingSquarePage.tsx:132-135`

**10. 通知页面缺少分页或无限滚动** — 通知只加载一次，老通知完全看不到，没有"加载更多"按钮。`NotificationsPage.tsx:53-56`

**11. 树洞页面的"我的"筛选依赖 localStorage 存储匿名码** — 用户换设备或清理浏览器数据后，"我的"筛选完全失效，原来的帖子无法找回。`TreeHolePage.tsx:14-30`

**12. 失物招领列表的详情入口不醒目** — 每个条目的可点击区域是整个卡片但没有 hover 态或其他可点击的暗示（移动端无 hover），用户可能不知道可以点进去看详情。`LostFoundList.tsx:66`

**13. 用户主页的"商品/帖子/笔记/评价"四个 Tab 下各自独立加载数据** — 用户每切一次 Tab 都要等待网络请求，切换体验卡顿且不可预测。`UserProfilePage.tsx`

**14. 对比功能入口隐晦** — 商品列表卡片右下角有一个微型"⇆ 对比"按钮（`text-[10px] backdrop-blur-sm`），用户几乎看不见，且点击后只会弹 toast，没有引导到对比页面。`GoodsListPage.tsx:265-276`

## 二、反馈缺失 · 用户不知道发生了什么（15–26）

**15. 发布商品后没有明确告知"审核中"** — `PublishGoodsPage` 提交成功后 toast "发布成功" 并跳转到"我的商品"，但用户看到商品列表里没有刚发布的商品（因为 pending 状态可能不展示），会以为发布失败了。`PublishGoodsPage.tsx:122-125`

**16. 评论提交后没有即时显示** — `GoodsDetailPage` 的评论发送后，如果状态为 pending 或 rejected，列表里看不到自己的评论，用户会反复提交。`GoodsDetailPage.tsx:188-200`

**17. 树洞点赞是乐观更新但失败时回滚没有 toast 提示** — 用户点赞后看到红心亮起，滚动离开再回来发现红心又灭了，没有任何错误提示。`TreeHolePage.tsx:94-114`

**18. 删除通知没有确认弹窗** — 通知列表每条通知右侧有一个垃圾桶图标，点击直接删除且只 toast "已删除" 1.5秒，用户误触后无法撤销。`NotificationsPage.tsx:87-95`

**19. 购物车操作反馈不一致** — 商品详情页从购物车移除时 toast "已从购物车移除"，但列表页的快照对比按钮没有购物车状态的视觉提示。`GoodsDetailPage.tsx:138-152`

**20. 页面初次加载时白屏或灰屏时间超过 2 秒无任何进度指示** — 部分页面（如 `ExplorePage`）的 loading 状态只在数据区域有骨架屏，但头部和底部 TabBar 渲染出来了，中间大片区域空白。`ExplorePage.tsx:176-186`

**21. 下拉刷新触发后执行 `window.location.reload()`** — 这会导致整页重载、JS 重新初始化，页面会闪烁白屏后再出现，完全没有"刷新数据"的平滑感。`LiquidGlassLayout.tsx:139`

**22. 图片上传没有进度条** — `ImageUploader` 上传图片时只有组件内部状态，用户看不到上传百分比，大图上传时没有任何反馈只能干等。`PublishGoodsPage.tsx:55-57`

**23. 恋爱请求发送后按钮状态没有即时更新** — 用户点击"💌 发送恋爱请求"后按钮不变成"已发送"或灰掉，用户可以反复点击发送重复请求。`DatingSquarePage.tsx:92-103`

**24. 搜索页的搜索建议和历史记录混在同一个区域** — 用户输入关键字时，下方同时展示"热门搜索"和"搜索历史"，且输入过程中就触发 API 请求，导致列表闪烁。`SearchPage.tsx:136-167`

**25. 通知页面没有"未读/已读"筛选** — 所有通知混在一起，用户想只看未读通知需要逐条扫过带蓝色小圆点的条目。`NotificationsPage.tsx`

**26. 联系人搜索弹窗关闭后再打开，之前的搜索结果丢失** — `AddFriendModal` 的 state 在关闭时没有清空，下次打开仍显示旧结果，但实际搜索框已为空。`ConversationsPage.tsx:175-183`
## 三、内容不可读 · 文字/图片看不清或无法辨别（27–38）

**27. 商品详情页轮播图的图片指示器在 Swiper 组件内部，但 CSS 中 `.swiper-pagination-bullet` 的颜色被设为 `rgba(0,0,0,0.25)`** — 在暗色图片上几乎看不见分页圆点。`liquid-glass-light.css:222-223`

**28. 深色模式下聊天气泡颜色依赖不存在的 CSS 变量** — `ChatPage` 使用 `bg-[var(--color-chat-bubble-self)]` 和 `bg-[var(--color-chat-bubble-other)]`，但这两个变量在液态玻璃 CSS 中没有定义，气泡背景会退化为透明。`ChatPage.tsx`，所有液态玻璃CSS

**29. 商品卡片上的"图片审核中"提示使用了黄色半透明背景** — 在浅色图片上黄色文字和背景几乎和图片融为一体，用户根本看不到这个重要提示。`GoodsListPage.tsx:253-256`

**30. 已售商品在列表中用 `bg-gray-800/80 text-white` 的半透明深色标签** — 在深色商品图片上，这个标签完全不可见。`GoodsListPage.tsx:258-260`

**31. 树洞帖子的匿名码显示为 `#XXXX`** — 经过 base64 编码后是如 `#a3Fk` 的字符串，对用户来说毫无意义，而且有些帖子的码完全相同，用户误以为是同一个人。`TreeHolePage.tsx:178`

**32. 液态玻璃 `LiquidGlassDemo` 中所有消息气泡在深色模式下颜色不变** — 气泡背景硬编码 `#F0F0F5`、`#0066D6`、`#0F172A`，暗色模式下气泡与深色背景无区分。`LiquidGlassDemo.tsx:189-192, 213-216`

**33. 标签切换按钮的非激活态使用 `#888888` 颜色** — 在浅色主题下这个颜色偏浅难以辨认，在深色主题下与背景对比度不足，两个主题可读性都很差。`liquid-glass.css:260`

**34. 底部 TabBar 标签文字 10px `text-transform: uppercase`** — 中文做了 uppercase 虽然没实质影响，但 10px 字体在中文显示下非常小，部分 Android 设备上文字糊成一条线。`liquid-glass.css:297`

**35. 商品详情页"查看卖家联系方式"按钮与下方联系方式之间的视觉关系不明确** — 用户点击后联系方式直接出现在按钮原位置下方，没有过渡动画，用户可能没注意到变化。`GoodsDetailPage.tsx:315-338`

**36. 用户主页评价 Tab 的星级条使用固定宽度的百分比条** — 当某个星级为 0 时，条宽度为 0%，用户看不到那个星级行的存在，评分分布图不完整。`UserProfilePage.tsx:145-152`

**37. 失物招领列表中悬赏金额使用 `text-[10px] text-amber-500`** — 这个字号和颜色在卡片中极易被忽略，悬赏是吸引用户帮助寻找的关键信息却被视觉弱化。`LostFoundList.tsx:77`

**38. 发布页"快捷模板"按钮中的 emoji 在部分 Windows 系统上渲染异常** — 📚💻👔 等 emoji 在 Windows 10 上显示为轮廓图标或黑白符号，与设计预期不符。`PublishGoodsPage.tsx:128-133`

## 四、表单填写 · 操作费力且容易出错（39–53）

**39. 发布商品时"求购"类型下仍然显示"成色"选择器** — 求购是表达需求而非描述现有物品，"成色"字段没有意义却占据了表单空间。`PublishGoodsPage.tsx:161-168`

**40. 价格输入框的类型是 `number` 但在移动端弹出的数字键盘没有小数点** — 用户输入 `2.5` 时需要切换到符号键盘，增加了输入步骤。`PublishGoodsPage.tsx:169-173`

**41. "以物易物"的交换物品选择使用 `prompt()` 弹窗要求输入商品 ID** — 用户需要记住一个数字 ID，在商品列表和交换弹窗之间来回切换，体验极差。`GoodsDetailPage.tsx:236-237`

**42. 树洞发布时输入框只到 1000 字符上限，但提示文字是 `{content.length}/1000`** — 用户打字到快满时才开始注意计数，没有在接近上限时给予醒目提示（如字数变红色）。`TreeHolePage.tsx:155`

**43. 登录页的用户协议勾选框不是标准的 `<input type="checkbox">`** — 是一个自定义 div + 内嵌 SVG，屏幕阅读器和键盘操作都无法正常使用，且勾选框尺寸（16px）在手机上偏小难以点中。`LoginPage.tsx:122-132`

**44. 草稿恢复弹窗使用原生 `window.confirm()`** — 在移动端 WebView 中，这个弹窗的样式与 App 风格完全不一致，且只有"确定/取消"两个选项，用户可能误点取消导致草稿丢失。`PublishGoodsPage.tsx:40-47`

**45. 发布商品的时间选择使用 `datetime-local` 输入框** — iOS Safari 对这个输入类型的支持不完整，部分设备上退化为普通文本输入框，用户需要手动输入日期格式。`PublishGoodsPage.tsx:152-159`

**46. 密码重置流程的三个步骤之间没有进度指示器** — 用户不知道当前在第几步、还有几步，只能通过界面内容的变化来猜测。`LoginPage.tsx:200-278`

**47. 商品筛选的价格输入框没有联动验证** — 用户输入最低价 100、最高价 50 时没有任何提示错误，搜索结果为空用户困惑。`GoodsListPage.tsx:209-220`

**48. 通知页面的多选模式中，选中条目没有总数提示** — 用户勾选了若干条后想知道选了多少条，必须自己去数。`NotificationsPage.tsx:161-164`

**49. 发布帖子的图片上传后，删除按钮是一个微小的红色 ×** — 在移动端很难精确点到 20×20px 的删除按钮，常常误触图片本身。`SquarePage` 的发布组件和 `DatingSquarePage` 均有此问题。

**50. 聊天输入框的 placeholder 为空字符串 `placeholder=""`** — 用户看不到任何输入提示，不知道该输入框的用途（虽然上下文是聊天，但空 placeholder 不符合通用设计惯例）。`ChatPage.tsx:501`

**51. 搜索历史中的删除按钮只在 hover 时显示** — 移动端没有 hover，用户永远看不到删除按钮。`SearchPage.tsx:149`

**52. 失物招领发布的"丢失/捡到"分类在表单中不显眼** — 两个按钮同等权重且样式平淡，用户在填写过程中可能忘记选择分类导致提交错误。

**53. 编辑商品时没有保留原有的审核状态提示** — 用户修改一个已审核通过的商品并保存后，不知道商品会重新进入审核队列，可能以为立刻就能展示。`PublishGoodsPage.tsx`

## 五、错误和空状态 · 出问题后用户不知所措（54–64）

**54. 商品详情页加载失败时只 toast "加载商品信息失败"，页面显示"商品不存在或已下架"** — 用户无法区分是网络问题还是真的已下架，可能反复刷新或放弃。`GoodsDetailPage.tsx:88`

**55. 搜索无结果时只显示"未找到相关结果"** — 没有推荐替代关键词、没有引导用户切换搜索类型、没有展示热门搜索作为备选。`SearchPage.tsx:112`

**56. 网络断开时整个应用没有任何全局离线提示** — 用户滑动浏览时请求静默失败，列表一直显示骨架屏或旧数据，用户不知道是网络问题。

**57. API 请求被限流或返回 429 时，前端没有限流重试和用户提示** — 用户快速切换分类筛选时可能触发限流，之后一段时间的请求全部失败但没有告知用户等待。

**58. 图片上传失败只 toast "上传失败"** — 没有说明原因（文件过大？格式不支持？网络超时？），用户无从修正。`GoodsDetailPage.tsx:188-198`

**59. 评论被 AI 审核拒绝后显示"已拒绝"标签，但 toast 为"评论成功"** — 两个信息矛盾，用户不知道评论到底有没有发出去。`GoodsDetailPage.tsx:192-196`

**60. 树洞页面全部帖子加载失败时显示"树洞里空空的"并用"说出你的第一句话吧"引导** — 实际上可能是网络问题导致无法加载，用户误以为真的没人发帖。`TreeHolePage.tsx:171-174`

**61. 购物车为空时没有任何引导** — 只展示空状态，没有"去逛逛"按钮或推荐商品，用户被困在空购物车里。`CartPage`

**62. 通知列表为空时的引导文案过于笼统** — "暂无通知"没有告诉用户什么情况下会收到通知（收藏的商品降价？有人评论？），新用户不知道通知能做什么。

**63. AI 审核轮询最多 4 次 × 3 秒 = 12 秒，超时后直接放弃** — 用户提交内容后等了 12 秒看到内容仍处于"审核中"，之后再也没有更新，无超时提示。`GoodsDetailPage.tsx:66-82`

**64. 404 页面没有返回首页的快捷入口** — `NotFoundPage` 展示"页面不存在"但没有导航按钮，用户需要手动修改 URL 或使用浏览器后退。`NotFoundPage.tsx`

## 六、性能体感 · 用户觉得慢/卡/费电（65–72）

**65. 首页加载同时请求多个 API（newest/hot/categories/announcements/banners）再触发筛选请求** — 用户进入首页后看到骨架屏持续闪烁多次数据才稳定，感知加载时间远超实际网络耗时。`HomePage.tsx`

**66. SquarePage 一次性并行请求 8 个 API 端点** — 移动端弱网环境下，8 个请求排队等待造成页面在骨架屏状态停留 5-10 秒。`SquarePage.tsx:169-195`

**67. 广场的瀑布流数据在前端做 `Math.random()` shuffle** — 这是 O(n) 的原地随机排序，100+ 条数据时在主线程阻塞导致页面滚动卡顿（jank）。`SquarePage.tsx:201-209`

**68. 液态玻璃的每个卡片、按钮、输入框都使用 `backdrop-filter: blur()`** — 一个页面 30+ 个毛玻璃元素在低端手机上 GPU 合成层过多，导致滚动帧率低于 30fps 且手机发热。`liquid-glass.css`

**69. 页面过渡动画使用 `AnimatePresence mode="wait"`** — 前一个页面必须完全退出动画完成后下一个页面才进入，在慢速设备上页面切换有 0.3-0.5 秒的空白间隔。`LiquidGlassLayout.tsx:146-152`

**70. 商品列表页的分类横向滚动实时监听 scroll 事件更新左右箭头状态** — 使用 `scroll` 事件而非 `IntersectionObserver`，滚动时每帧都触发 setState，造成不必要的重渲染。`GoodsListPage.tsx:40-51`

**71. 带图片的树洞帖子在展开评论区时没有懒加载优化** — 9 张图片的树洞帖展开评论后所有图片同时请求加载，低端设备可能出现短暂冻结。`TreeHolePage.tsx:180-186`

**72. 暗色模式 CSS 中使用了约 30 处 `!important` 强制覆盖** — 浏览器在样式重计算时需要逐条处理这些高优先级规则，页面切换或滚动时布局计算的性能开销增大。`liquid-glass-dark.css`
## 七、视觉不一致 · 同一个 App 像是不同人做的（73–83）

**73. 液态玻璃布局使用 iOS 26 风格圆角（16/20/24px），但内部页面大量使用 Tailwind 的 `rounded-xl`（12px）** — 同一个页面出现 12px、16px、20px 三种圆角，视觉不统一。

**74. 底部 TabBar 在主页面和子页面之间突然消失又出现** — `LiquidGlassLayout` 的 scroll 驱动隐藏/显示逻辑在页面切换时没有重置，TabBar 在不需要隐藏的页面上也突然消失了。`LiquidGlassLayout.tsx:112-123`

**75. 登录页使用自己的渐变背景动画（`#ee7752`→`#e73c7e`→`#23a6d5`→`#23d5ab`）** — 这个暖色调渐变与液态玻璃的冷色调 `#0F172A` 背景完全冲突，用户从登录页进入主页后视觉跳跃巨大。`LoginPage.tsx:69-78`

**76. 树洞页面使用白色毛玻璃卡片 `bg-white/80 backdrop-blur-xl`** — 但液态玻璃主题的卡片标准是 `rgba(255,255,255,0.65)` 加 `backdrop-filter: blur(16px)`，有两个不同的玻璃规范并存。`TreeHolePage.tsx:155`

**77. 恋爱空间页面使用粉色渐变背景 `from-pink-50 via-rose-50 to-purple-50`** — 与其他页面的蓝灰色调毫无关联，从首页跳到恋爱空间色彩差异过大。`DatingSquarePage.tsx:124, 131`

**78. 液态玻璃 Demo 使用 emoji 作为 TabBar 图标，但生产版本使用 SVG 图标** — 两个版本的图标体系完全不同：emoji vs SVG，风格割裂。`LiquidGlassDemo.tsx:455-461`

**79. "发布"按钮在 TabBar 中的样式：demo 中是蓝色圆角正方形 `w-9 h-9 rounded-xl`，生产版是圆形 `w-9 h-9 rounded-xl`** — 但 CSS 中 `.lg-tabbar-item-active` 是 `border-radius: 26px` 的胶囊形，三个版本各自不同。`LiquidGlassLayout.tsx:211-215`, `LiquidGlassDemo.tsx:487`

**80. 已售标签的颜色：列表页是 `bg-gray-800/80 text-white`，详情页是 `STATUS_MAP[sold].color`** — 两个地方同一状态使用了不同颜色体系。`GoodsListPage.tsx:258`, `GoodsDetailPage.tsx:273`

**81. 商品卡片的"出/求/租"标签在首页和列表页使用了不同的 HTML 结构** — 首页用 `<span>` + Tailwind 颜色类，列表页用 inline style 背景色，相同的标签有两种不同实现。

**82. 深色模式切换时，液态玻璃的 `.dark .lg-root` 类需要依附在 `<html>` 上** — 但 Layout 中没有在 `<html>` 上设置该 class，导致用户切换暗色模式后液态玻璃的暗色 CSS 完全不生效，页面看起来破碎。`liquid-glass-dark.css:7`

**83. 液态玻璃的 `lg-sheet` 在 iOS 上底部圆角被 safe-area 裁切** — `padding-bottom: calc(24px + env(safe-area-inset-bottom, 0px))` 只增加了内边距但没有处理 `border-radius: 24px 24px 0 0` 在 safe area 区域的视觉连续性。`liquid-glass.css:377-399`

## 八、跨设备体验 · 手机和电脑上表现不一致（84–90）

**84. 桌面端打开液态玻璃布局可以看到过窄的移动端模拟卡片** — 液态玻璃布局使用 `px-4` 但没有 `max-w-*` 桌面端居中约束，在 27 寸显示器上内容撑满全宽，间距夸张。`LiquidGlassLayout.tsx:144`

**85. 商品详情页在桌面端做了 `md:flex md:gap-6` 双栏布局** — 但底部操作栏在桌面端 `hidden md:block` 而在移动端 `fixed bottom-14`，用户在桌面端缩小浏览器到平板宽度时操作栏位置突变。`GoodsDetailPage.tsx:297-301`

**86. `ChatPage` 在液态玻璃主题下用 `fixed inset-0 z-50` 覆盖全屏** — 桌面端用户缩小窗口时，fixed 定位让聊天页面覆盖在其他内容上方，无法同屏浏览聊天和商品。`ChatPage.tsx:388`

**87. 底部 TabBar 在 Android 的 Chrome 中底部导航栏与系统导航条重叠** — `padding-bottom: calc(12px + env(safe-area-inset-bottom, 0px))` 在部分 Android 设备上 `env(safe-area-inset-bottom)` 为 0，导致 TabBar 被系统导航条遮挡。`liquid-glass.css:275-276`

**88. 键盘弹起时 `visualViewport` API 检测在部分 Android 浏览器上不触发** — TabBar 的隐藏/显示逻辑依赖 `.height < .innerHeight * 0.85` 的阈值比较，在不支持 visualViewport 的浏览器上 TabBar 始终显示，遮挡输入框。`LiquidGlassLayout.tsx:103-110`

**89. Swiper 轮播图在桌面端鼠标滚轮事件会同时触发页面滚动和 Swiper 切换** — 用户在轮播图区域滚轮时，预期滚动页面但触发了图片切换。`HomePage.tsx`

**90. `LiquidGlassDemo` 使用 `max-w-[390px] mx-auto` 模拟手机宽度** — 但右侧没有提示"这是手机预览"，桌面用户看到大量的左右留白误以为页面显示不全。

## 九、安全感和信任 · 用户不放心操作（91–96）

**91. "注销账号"按钮只有一个 `window.confirm()` 确认后直接调用 API** — 没有二次确认、没有告知注销后果、没有冷静期，用户误触后无法挽回。`MyProfilePage.tsx:175-179`

**92. 用户联系方式（微信/QQ）默认隐藏但点击"查看卖家联系方式"只做了一次点击确认** — 没有告知卖家"你的联系方式已被查看"，用户不知道自己的隐私是否安全。

**93. 举报功能在树洞的入口是一个极小的旗帜图标** — 且没有举报类别选择（直接 `reason: "违规内容"`），用户觉得举报功能形同虚设。`TreeHolePage.tsx:193-196`

**94. 发布商品时 AI 文字审核是静默进行的** — 用户不知道自己的商品描述是否通过了审核，如果被拒绝，仅显示"AI审核未通过"标签而没有具体违规点。`GoodsDetailPage.tsx:278`

**95. 评论审核被拒后显示"原因：{reviewComment}"** — 但这个 reviewComment 字段可能是空字符串或管理员未填写，用户看到空白的"原因："会更加困惑。`GoodsDetailPage.tsx:343-344`

**96. 登录页版本切换按钮位于表单下方很小的区域** — 用户如果误触了"液态玻璃"切换，页面布局完全改变，但没有"切回普通版本"的确认提示。`LoginPage.tsx:155-170`

## 十、内容发现 · 用户找不到有趣的内容（97–100）

**97. 首页的"热门推荐"和"最新发布"使用相同的 `GoodsItem` 组件** — 两个区块视觉上几乎一模一样，用户无法快速区分"热门"和"最新"的差异。`HomePage.tsx`

**98. 树洞没有"只看热门"以外的排序选项** — 只有"最新/最热"两个选项，无法按时间范围筛选（如"本周最热"），老帖子沉底后再也看不到。`TreeHolePage.tsx:160-163`

**99. 用户主页只展示了该用户的商品和帖子，没有展示"共同好友"或"共同交易"等社交关系线索** — 用户在判断是否信任一个陌生卖家时缺少参考信息。`UserProfilePage.tsx`

**100. 液态玻璃整体架构中 `LiquidGlassDemo` 是一个独立演示、`LiquidGlassLayout` 是生产布局，但两者的页面组件完全不同步** — 用户在登录页选择了"液态玻璃"被路由到 `/lg/*` 生产环境，但 Demo 中的很多视觉设计（如渐变特色卡片、AI 助手特殊样式）在生产环境不存在，用户看到的液态玻璃是一个残缺版。`LiquidGlassLayout.tsx` vs `LiquidGlassDemo.tsx`

---

> **统计**：导航迷失 14 条 · 反馈缺失 12 条 · 内容不可读 12 条 · 表单填写 15 条 · 错误/空状态 11 条 · 性能体感 8 条 · 视觉不一致 11 条 · 跨设备体验 7 条 · 安全和信任 6 条 · 内容发现 4 条。严重度最高的问题：暗色模式完全失效(#82)、聊天气泡颜色不可见(#28)、发布 Tab 跳转错误(#1)、下拉刷新导致整页重载(#21)、Demo 和生产版本断崖式差异(#100)。
